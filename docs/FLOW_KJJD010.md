# KJJD010 受注データ更新 JOBフロー

## 1. 基本情報

| **システム名** | **サブシステム名** | **ジョブ名** |
| -------------- | ------------------ | ------------ |
| 研修 (ID: K) | 受注 (ID: KJ) | 受注データ更新 (ID: KJJD010) |

## 2. フロー概要

受注データをチェックして、正常分の受注チェックファイルから売上ファイルを作成し、商品売上マスタを更新します。

## 3. JOBフロー

```mermaid
flowchart TD
    %% クラス
    classDef sub opacity:0
    classDef note fill:#ffd, stroke:#ccb, text-anchor:start

    %% 入出力ファイル・マスタ
    IN1[(受注データ<br/>行順編成)]
    M1[(商品マスタ<br/>SAMファイル)]

    %% 処理プロセス
    KJBM010[<strong>KJBM010</strong><br/>受注チェックファイル作成]
    KJBM020[<strong>KJBM020</strong><br/>受注データ形式チェック] 
    KJBM030[<strong>KJBM030</strong><br/>商品番号チェック] 
    KJBM050[<strong>KJBM050</strong><br/>受注データ振り分け]
    KUBM010[<strong>KUBM010</strong><br/>売上ファイル作成]
    KUBM020[<strong>KUBM020</strong><br/>売上集計] 
    KUBM030[<strong>KUBM030</strong><br/>売上更新]

    %% 処理フロー
    IN1 --> KJBM010
    KJBM010 --> D1
    D1[("受注チェックファイル")] --> KJBM020 
    KJBM020 --> D2
    D2[("受注チェックファイル")] --> U1
    U1[["ソートユーティリティ<br/>商品番号順"]]--> D3
    M1 --> KJBM030
    D3[("受注チェックファイル<br/>商品番号でソート済み")] --> KJBM030
    KJBM030 --> D6
    D6[("受注チェックファイル")] --> KJBM050
    KJBM050 -- "正常分" --> D5T
    D5T[("受注チェックファイル")] --> KUBM010
    KJBM050 -- "エラー分" --> F3[(受注チェックファイル<br>エラー分)]
    KUBM010 --> D7
    D7[(売上ファイル)] --> U2
    U2[["<strong>ソートユーティリティ</strong><br>商品番号・日付順"]] --> D8
    D8[("売上ファイル<br>商品番号・日付順")] --> KUBM020
    KUBM020 --> D9
    D9[("売上集計ファイル")] --> KUBM030

    %% グルーピング
    subgraph subKJBM010 [" "]
        KJBM010
        noteKJBM010[
・受注データから受注チェックファイルを作成します。 
・エラー区分は全てスペースクリアします。 
        ] 
    end

    class subKJBM010 sub
    class noteKJBM010 note
```

## 4. 各処理プロセス概要

### 受注チェックファイル作成 (KJBM010)

* 受注データから受注チェックファイルを作成します。
* エラー区分は全てスペースクリアします。

### 受注データ形式チェック (KJBM020)

* 受注データの各項目のデータ形式（属性）をチェックします。
* エラーの項目は、該当するエラー区分をセットします。

### ソート (ユーティリティ)

* 商品番号でソートを行います。
### 商品番号チェック (KJBM030)

* 受注データの商品番号を商品マスタとマッチングしてチェックします。
* すでに商品番号のエラー区分がセットされているものは、チェック対象外とします。
* マッチした場合は商品名をセットし、アンマッチの場合は該当エラー区分をセットします。

### 受注データ振り分け (KJBM050)

* 受注チェックファイルを正常分とエラー分に振り分けます。
* エラー区分が全てスペースであれば正常分として扱います。

### 売上ファイル作成 (KUBM010)

* 前工程（KJBM050）でチェック済みの「受注データ 正常分」から、売上ファイルを作成します。

### ソート (ユーティリティ)

* 売上ファイルを、商品番号、受注日付の順にソートします。

### 売上集計 (KUBM020)

* 商品番号、受注年月単位で売上金額を集計します。

### 売上更新 (KUBM030)

* 売上集計ファイルの売上金額を、売上マスタの「売掛現在残高」と「売上金額」へ加算（更新）します。
